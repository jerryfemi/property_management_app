import 'package:flutter/material.dart';

class AppColorsLight {
	// Primary 
	static const primary = Color(0xFF1B4FD8);
	static const primaryLight = Color(0xFFEEF2FF);

	// Accent — success, positive actions (Pay Rent, Available)
	static const accent = Color(0xFF10B981);
	static const accentLight = Color(0xFFECFDF5);

	// Semantic
	static const danger = Color(0xFFEF4444); // overdue, errors
	static const dangerLight = Color(0xFFFEF2F2);
	static const warning = Color(0xFFF59E0B); // expiring, pending
	static const warningLight = Color(0xFFFFFBEB);

	// Neutrals
	static const background = Color(0xFFF8FAFC); // app background
	static const surface = Color(0xFFFFFFFF); // cards, modals
	static const dark = Color(0xFF0F172A); // primary text
	static const muted = Color(0xFF64748B); // secondary text
	static const border = Color(0xFFE2E8F0); // dividers, outlines
}

class AppColorsDark {
	// Primary — trust, stability
	static const primary = Color(0xFF1B4FD8);
	static const primaryLight = Color(0xFF233B74);

	// Accent — success, positive actions (Pay Rent, Available)
	static const accent = Color(0xFF10B981);
	static const accentLight = Color(0xFF064E3B);

	// Semantic
	static const danger = Color(0xFFEF4444); // overdue, errors
	static const dangerLight = Color(0xFF7F1D1D);
	static const warning = Color(0xFFF59E0B); // expiring, pending
	static const warningLight = Color(0xFF78350F);

	// Neutrals
	static const background = Color(0xFF09090B); // app background
	static const surface = Color(0xFF171717); // cards, modals
	static const dark = Color(0xFFE5E7EB); // primary text
	static const muted = Color(0xFF94A3B8); // secondary text
	static const border = Color(0xFF262626); // dividers, outlines
}